sudo: false
dist: trusty
language: cpp
compiler:
- gcc

addons:
apt:
sources:
- ubuntu-toolchain-r-test
packages:
- g++-7
- cmake

install:
- if [ "$CXX" = "g++" ]; then export CXX="g++-7" CC="gcc-7"; fi

script:
- mkdir build
- cd build
- cmake ..
- make
